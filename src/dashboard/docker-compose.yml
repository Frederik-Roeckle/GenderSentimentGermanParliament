version: '3.8'
services:
  nlpdashboard:
    build:
      dockerfile: Dockerfile
    container_name: nlpdashboard
    ports:
      - "8999:8051"
    networks:
        - public

networks:
  public:
    name: public
    external: true